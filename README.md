# ReactJS Calculator

This is a simple calculator application done using only ReactJS with Bootstrap as the CSS framework.

To run this in your local environment simply run:

    npm start

or if you use yarn:

    yarn start